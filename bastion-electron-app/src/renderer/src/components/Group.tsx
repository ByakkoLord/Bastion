import Note from './Note';

export default function Group(): JSX.Element {
    return (
        <section id="group">
            <h2 id="group-name">Empresa</h2>
            <Note />
            <Note />
        </section>
    )
}